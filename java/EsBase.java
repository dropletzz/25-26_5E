import java.util.*;

public class EsBase {
    /*
     * Calcola la somma di tutti i numeri pari presenti nell'array.
     * Se l'array è vuoto o non contiene numeri pari, ritorna 0.
     * * Ad esempio:
     * sommaPari(new int[]{1, 2, 3, 4, 5, 6});
     * ritorna: 12  (cioè 2 + 4 + 6)
     */
    public static int sommaPari(int[] numeri) {
        return -1;
    }

    /*
     * Ritorna true se il numero n è un numero primo, altrimenti false.
     * Ricorda: un numero primo è maggiore di 1 ed è divisibile solo per 1 e per se stesso.
     * * Ad esempio:
     * ePrimo(7);  ritorna: true
     * ePrimo(10); ritorna: false
     */
    public static boolean ePrimo(int n) {
        return false;
    }

    /*
     * Conta e ritorna il numero di vocali (a, e, i, o, u) presenti nella stringa s.
     * Fai attenzione: deve contare sia le vocali minuscole che quelle maiuscole!
     * * Ad esempio:
     * contaVocali("ciao mondo");
     * ritorna: 5
     */
    public static int contaVocali(String s) {
        return -1;
    }

    /*
     * Riceve una lista di numeri interi e ritorna una NUOVA lista
     * contenente solo i numeri strettamente maggiori di zero (positivi).
     * * Ad esempio:
     * filtraPositivi([-1, 5, 0, -3, 8]);
     * ritorna: [5, 8]
     */
    public static ArrayList<Integer> filtraPositivi(ArrayList<Integer> lista) {
        ArrayList<Integer> risultato = new ArrayList<>();
        
        return risultato;
    }

    /*
     * Date due liste di interi l1 e l2, ritorna una nuova lista che contiene
     * solo gli elementi presenti in ENTRAMBE le liste (l'intersezione).
     * Puoi assumere che le liste di partenza non contengano duplicati al loro interno.
     * * Ad esempio:
     * intersezione([1, 2, 3, 4], [3, 4, 5, 6]);
     * ritorna: [3, 4]
     */
    public static ArrayList<Integer> intersezione(ArrayList<Integer> l1, ArrayList<Integer> l2) {
        ArrayList<Integer> risultato = new ArrayList<>();
        
        return risultato;
    }
}
